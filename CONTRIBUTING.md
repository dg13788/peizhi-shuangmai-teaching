# 贡献指南（CONTRIBUTING）

感谢你关注「培智·双脉教学引擎」开源项目。本指南与 SKILL.md 配套，
请先阅读 SKILL.md（尤其 §0 五条铁律与低打扰协议）与 LICENSE 再参与贡献。

## 1. 贡献边界（专精特教）

本项目坚持**"不拼通用、专精培智、沉淀方法论"**定位：

- ✅ 欢迎：培智学校（及同类个别化教学）学科备课流程的改进、LOS 分层策略优化、
  趣味化/参与式学习活动库扩充、安全替代与 AAC 沟通支持补充、文案与格式修正、
  示例教案实跑验证与新增。
- ❌ 拒绝：混入普通教育通用备课逻辑的 PR；削弱红区护栏的 PR；承诺疗效或
  医疗/康复建议的内容；含真实学生个人信息的任何内容。

## 2. 贡献流程

1. **Fork** 本仓库到个人账号。
2. 创建功能分支（如 `feature/xxx`、`fix/xxx`），命名清晰描述变更。
3. 完成修改，并**同时**更新：
   - 对应版本号（SemVer 三段格式，规则见 `CHANGELOG.md` 头部）；
   - 外部 `CHANGELOG.md`（日期/版本号/变更类型/变更说明）；
   - 若新增/修改环节破坏了示例教案，视为**破坏性变更**，须升大版本并更新示例。
4. 提交 PR，PR 描述必须附带：
   - 变更说明（改了什么、为什么改）；
   - 对应版本号；
   - 第 3 节回归测试最小集结果（九项逐条打钩）。
5. 维护者将按仓库流程评审。

## 3. 回归测试最小集（发布前必跑）

PR 合并前必须通过以下九项（详见 `docs/release-checklist.md` 第八节）：

- [ ] 示例课题实跑 1 篇，产出一致（每课时五列表〔含 BOPPPS 步别、≥1 个探究活动、
      时间合计=该课时时长〕、分课时目标矩阵、LOS 表逐课时成对列且全生覆盖）
- [ ] 附件识别回归：内容提取卡七字段齐全（含红区拦截记录）、回填环节 0/1 生效、
      红区拦截生效
- [ ] Word+PDF 双格式生成成功，`SKILL.md` §4 格式与无障碍基线核验全过
      （字号下限、对比度 ≥7:1、色彩不得单靠颜色区分、黑体无衬线、表格有表头不溢出）
- [ ] 多课时教案必带**跨课时行为干预支持卡**六要素齐全、强化计划逐课时削弱、
      晋级降级阈值含 80% 判据、危机处置四条红线（禁体罚/禁惩罚性隔离/禁强制进食/禁当众批评）
- [ ] 机器可读产物合 `docs/output-schema.json`（`anchors` 每条三级齐全，含 timeline 步别枚举与逐课时时间恒等硬约束）
- [ ] **Single Source**：结构化 JSON 已由 md 源稿经 `python docs/md_to_json.py` 派生（**禁止手工编辑 JSON**），且"落盘样例≡派生结果"PASS
- [ ] **Word 成品**：已用 `python docs/md_to_docx.py` 生成（**禁手工排版 markdown 产物**），
      落盘 docx ≡ 源稿派生且字节幂等；需要时加 `--pdf` 导出（三通道皆无时须给出降级指引）
- [ ] `python docs/regression_check.py` 全项 PASS，报告已更新至
      `docs/regression_report.txt`（通过率必须为 100%，项数以报告为准；含引擎自身防漂移校验：
      frontmatter 字段、SemVer、版号多处一致、五条铁律表述、外部引用存在性；
      含 md→JSON 派生一致性与 Word 成品结构/版式/反篡改）
- [ ] 全库扫描无真实姓名/照片/病历（红区零输入）

## 4. 红区护栏红线（不可协商）

- **红区护栏相关改动一律拒绝**：任何移除、弱化、绕过"红区零输入"护栏的提交
  （含通过附件绕过、输出未脱敏信息、默认编造学情等），一律拒绝并提醒。
- 提交内容不得含真实学生姓名、身份证号、病历、照片、家庭敏感结构；
  示例与文档一律使用代号（生1/生2…）或占位符（`{{字段名}}`）。

## 5. 维护者守则

- 接受 PR 但不承诺响应时效（SLA）。
- 重大方向变更（如改变六环节/双脉/Gate/四视图主干）须先在 ISSUE 讨论，
  不接受未经讨论的破坏性主干变更。
- 合入前须复核：版本号与 CHANGELOG 是否同步、回归测试是否通过、
  全库红区扫描是否干净。

## 6. ISSUE 模板

提交 Issue 请包含以下信息：

```
- 平台/环境：（如 Marvis / 豆包 / ChatGPT / 本地文件系统等）
- 触发场景：（如"备一节培智二年级生活数学课，课题：5 的认识"）
- 期望 vs 实际：（期望行为与实际情况的差异）
- 是否含真实学生信息：（若含，将拒绝并提醒脱敏）
```

> 若 Issue 或 PR 中包含真实学生信息，维护者将拒绝处理并提醒脱敏后重新提交。
