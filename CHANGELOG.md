# CHANGELOG

本文件维护「培智·双脉教学引擎（特教 IEP 备课 Skill）」的版本变更记录。
版本号规则：**SemVer 1.0.0 三段格式（主版本.次版本.修订号）**；元数据/文案修正递增修订号，
结构性重构升大版本，历史条目号已按 SemVer 规范化（1.0→1.0.0、1.1→1.1.0、2.0→2.0.0）。

## [2.5.0] - 2026-09-25

> 技能上传规范专项整改。对照火山引擎 AgentKit / 豆包技能平台上传校验口径（兼顾问类规范的
> 千问、WorkBuddy），在不改变核心功能的前提下完成合规改造；并确立**双轨制**：
> 仓库保留 GitHub 治理文件（开发态），上传 zip 由打包器从仓库产出（上传态），两轨同源。

### 头号工作 · 双轨合规（技能平台上传规范 × GitHub 仓库规范）
- **frontmatter 对齐规范**（原 P0 阻塞项：缺 `description` 会被平台判缺字段）：
  - 新增必填 `description`（沿用原 description_zh 内容，≤1024 字符、无 XML 标签）；
  - 顶层字段收敛到规范白名单：`name / description / version / license / compatibility / metadata`；
    旧非标顶层字段（display_name、display_name_en、description_zh、description_en、tags、
    requires、author）全部移入 `metadata`（规范允许的 string→string 映射），信息不丢失；
  - `name` 维持 peizhi-shuangmai-teaching（小写+连字符、≤64、不含 agentkit）。
- **标准布局**：`docs/` 三脚本迁 `scripts/`，`output-schema.json` 与 `release-checklist.md`
  迁 `references/`；SKILL.md §4 格式基线完整条文拆至 `references/format-baseline.md`
  （渐进式披露，SKILL.md 只留核验摘要，≤500 行）。
- **新增 `scripts/build_package.py` 打包器**：一键产出 `dist/peizhi-shuangmai-teaching.zip`
  ——顶层技能名目录，仅含 SKILL.md＋scripts/＋references/（含 LICENSE 复制件）＋examples（md+JSON）；
  自动排除治理文件、`.git/.workbuddy/dist`、`*_report.txt`、`*.docx/*.pdf`；打包前强制执行
  frontmatter 必检（name/description 合规）与**入包全文红区扫描**（命中即拒绝打包）；
  固定 zip 时间戳，同一仓库状态产出逐字节恒定。
- **GitHub 轨保留**：README / CHANGELOG / CONTRIBUTING / LICENSE / .gitignore 全部保留并同步 2.5.0；
  LICENSE 在打包时自动复制为包内 `references/LICENSE.md`，许可随包分发。

### P0/P1 · 本轮实跑后新发现并修复
- **`md_to_docx.py` 不创建输出目录**：任意指定 `<成品目录>` 时生成静默失败、`--check` 全 DIFF
  （旧默认目录 examples 恒存在，属高隐蔽性缺陷）→ 生成模式自动 `makedirs`。
- **运行期报告污染包体**：`*_report.txt` 原落 `docs/` 会被一并打包/入库 → 三脚本统一改写
  系统临时目录 `%TEMP%/peizhi_shuangmai/`。
- **examples/ 二进制成品**：`*.docx` 属"不支持的文件类型"且非技能资产 → 出包出库
  （回归"成品≡源稿"端到端反篡改校验改在临时目录执行，校验力不降）；`.gitignore`
  移除 `!examples/*.docx` 白名单，新增 `dist/`。
- **回归脚本适配双轨**：新增 frontmatter 合规断言组（必填/白名单/name 规则/description ≤1024
  无 XML）、技能布局断言（scripts/references 齐全、docs/ 已移除、无二进制、无报告件）、
  GitHub 轨断言（治理文件保留、README 标题与 CHANGELOG 条目同版本）；全部路径断言更新。

### 兼容性说明
- 引擎主线"六环节＋双 Gate＋四视图＋双格式"零变更；示例教案 md 未改动（仅派生 JSON 随
  schema_version 同步）；CLI 用法仅默认输出目录参数语义不变、报告位置变化。
- 版本号六处一致：frontmatter `version` / 正文标题 / `references/output-schema.json` const /
  两脚本 `ENGINE_VERSION` / CHANGELOG 本条目（示例 JSON 随派生同步）。

## [2.4.0] - 2026-09-24

> 第四轮三视角主动回测。本轮把上一轮自评中**扣分最重的一项**（排版视角"只有文本基线、
> 无可执行的 Word 生成器"）做成了代码，并在实跑后又揪出一批治理与包完整性缺陷。

### 头号工作 · 格式基线代码化（排版视角最大缺口已补）
- 新增 **`docs/md_to_docx.py`**：md 源稿 → Word 成品的**唯一执行通道**。
  - **零第三方依赖**（纯标准库写 OOXML）、**字节级幂等**（固定压缩时间戳与部件顺序）；
  - 版式由代码强制：A4 与页边距 2.54/3.18cm、正文宋体/Times New Roman 小四 1.5 倍行距、
    段首缩进 2 字符、一级标题黑体三号加粗、表格字号 ≥9pt、表头加粗居中且跨页重复、
    表格宽度合计=版心、五列表列宽比 14/26/20/24/16、封面独立成节不出现页码、
    页脚居中「第 X 页 共 Y 页」（PAGE/SECTIONPAGES 字段，正文起始页码=1）；
  - 生成前红区扫描，**命中即拒绝落盘**；`{{}}` 占位符原样保留；**成品零引擎元信息**
    （含 `docProps/core.xml` 只写课题标题，不写作者/引擎/版本）；
  - CLI：`python docs/md_to_docx.py`（生成）／`--pdf`（Word COM → docx2pdf → LibreOffice
    依次尝试，皆无则给出降级指引而不阻断交付）／`examples --check`（成品≡源稿一致性校验）。
- §3.5 明令**禁止在上下文内手工重排 markdown**，成品一律走脚本；§4 标注"本基线已代码化"。

### P0/P1 · 本轮实跑后新发现并修复
- **`.gitignore` 与回归基准冲突**：`*.docx` 被忽略，而回归断言"落盘 docx ≡ 源稿派生"依赖它，
  克隆后校验必然失败 → 增加白名单 `!examples/*.docx`，并修正注释中已失效的旧章节号引用。
- **OOXML 元素序列违规 4 处**（Word 对序列敏感，顺序错会触发"内容不可读"修复提示，
  而此前所有断言只做子串匹配、全部放行——属**高隐蔽性假阴性**）：
  ① `sectPr`：`footerReference` 必须在 `pgSz` 之前、`pgNumType` 必须在 `cols` 之前；
  ② `tblPr`：`tblLayout` 必须在 `tblBorders` 之后；③ `trPr`：`cantSplit` 必须在 `tblHeader` 之前；
  ④ `pPr`：`shd` 必须在 `spacing/ind/jc` 之前。已全部修正，并新增**顺序型断言**（按索引递增判定）。
- **OOXML 包缺 `docProps/core.xml`**：包虽可被解析，但缺文档属性部件；已补齐并声明
  Content-Type 与 package 关系，同时保持零引擎元信息（时间戳用固定常量以维持字节幂等）。
- **SKILL.md §6 顶层字段声明漏 `schema_version`**（文档 11 项 vs 契约 12 项）：已补齐，
  并新增"§6 声明 ≡ Schema required"断言，文档与契约再不同步即 FAIL。
- **治理文件版本漂移**：README 标题与版本段仍为 2.3.0、CHANGELOG 缺 2.4.0 条目、
  CONTRIBUTING/上架清单仍写死旧回归项数 → 全部同步，并**取消硬编码项数**（改为引用报告）。
- **临时调试件残留**：`docs/_*.py|txt|json` 共 5 个（含 2.2.0 手工样例备份，属版本漂移源）已清除。
- **回归脚本自身缺陷**：元断言"重复断言"按全局去重，把跨文件的合法同名断言误判为重复 →
  改为按分节去重；子进程读取在 Windows GBK 控制台下抛解码异常 → 显式 utf-8 解码。

### 增强 · 特教专业机制
- **五维感官调节前置**（前因层先于任务与强化调整）：听觉/视觉/前庭与座位/口欲与过敏/触觉与材料，
  配《感官调节与环境安排表》四列（维度/触发信号/前置安排/课中降刺激通道），
  降刺激通道须立即可执行且不具惩罚性，**禁以冷静角/隔离作为唯一通道**。
- **IEP 长期目标跨课时累计追踪表**：学生代号/年度长期目标/本课短期目标/分课时达成/累计成功率，
  全生覆盖，禁止只记最后一课时；≥80% 触发支持降级评估。
- **Gate 话术模板**（防跨会话话术漂移）：Gate-A/Gate-B 各给可照抄模板与"一次只问 1~2 项"通则。

### 增强 · Skill 架构
- **上下文预算策略**：一次一课时串行生成、N>3 拆分多锚定单、压缩优先级
  （目标矩阵/五列表/LOS/IEP 追踪/安全替代/支持卡一律不可压缩）、不在上下文内做格式转换。
- **幂等与产物一致性**：同源派生、字节幂等、改动顺序（先 md 后产物）、重跑全量重写禁追加。

### 回归
- `docs/regression_check.py` 扩至七组：教案基准 / 结构化契约 / 引擎自身防漂移 /
  md→JSON 派生一致性 / **Word 成品（包完整性·元素序列·版式·幂等·反篡改·外部读取）**
  ＋交付通道契约＋仓库卫生；**发布前须 100% PASS，项数以报告为准**（治理文档不再硬编码项数）。
- 新增**端到端反篡改测试**：篡改成品字节后 `--check` 必须报 DIFF；PDF 通道不可用时必须
  返回 None 且不抛异常；仓库卫生断言防止临时件入包、防止治理文档写死过期项数与旧章节号。

## [2.3.0] - 2026-09-23

> 第三轮三视角主动回测（不复用上一轮结论，重新做批判性审查）。
> 本轮最重要的发现是：**2.2.0 由我手工编写的 JSON 样例违反了 Single Source 铁律**——
> 用新写的派生器比对后，证实它与 md 源稿存在实质性语义漂移。

### P0 · Single Source 违规（自纠）
- 手工样例 `examples/好吃的水果_结构化输出样例.json` 与 md 源稿漂移：**丢失"阅读"领域课标锚点**
  （md 有 3 条锚点，JSON 压缩为 1 条并丢弃"阅读"）、分层作业结构不对齐、教材多处字段失真；
  且每次升版需手工维护两份，漂移必然复发。
- 新增 **`docs/md_to_json.py`**：md → 结构化 JSON 的**唯一派生通道**（解析教案头 / 多锚点 / 课时总体安排 /
  目标矩阵 / 五列表 timeline / 探究活动 / 板书 / LOS 记录表 / 安全替代 / 生活泛化 / 分层作业 / 材料 /
  AAC / 行为支持卡 / 判定阈值表），**禁止手工编写 JSON**；§6 写入 Single Source 硬约束与版本锁约定。
- 回归脚本新增 **md→JSON 派生一致性**断言组（含"落盘样例 ≡ 派生结果"），一旦漂移立即 FAIL。

### P1 · 回测发现并修复
- **表格缺语义表头**（违反 §4"表格必有表头行"）：两篇示例的"分层作业"表首行直接是数据
  （`| 第1课时·A | …`），转 docx 会错位；已补 `| 层次（课时·组） | 作业内容（含家长配合项） |`。
- **五列表表头漂移**：示例首列写成"教学环节（时间·步别）"，与引擎规定的"教学环节"不一致且列名冗余、
  误导 Word 列宽；已统一为 `| 教学环节 | … | 支持策略（A/B/C 分层） |`。
- **危机处置缺上报链**：示例写了"必要时启动学校应急预案"，引擎正文却无——**引擎弱于示例属危险反向依赖**，
  已补"必要时启动学校应急预案并按校规上报（校医 / 班主任 / 家长，记录上报对象与时间）"，
  并把呛噎过敏纳入触发情形。
- **目标判据无书写规范**，不同模型颗粒度漂移：已规定**判据三段式** `〔条件/支持〕＋动作＋〔次数或正确率〕`，
  禁只用"掌握/了解/知道"等不可测词。

### P2 · 修订
- Schema `anchor`（单对象）→ **`anchors`（数组）**：一课常挂多个领域锚点（md 实测 3~4 条），原设计无法承载。
- 家校反馈视图增"**附 1 条家庭可用的行为一致性做法**"，补上 PBS 跨情境一致性的最后一环。
- 强化削弱计划补充 N>3 的三段循环规则（中段维持 FR3/VR2，末段淡出）。
- 修复两篇示例材料清单段末缺失空行（md 排版小瑕疵）。

### 工具链
- `docs/regression_check.py` 断言 **88 → 118 项**：新增"表格首行非数据行""五列表表头严格匹配"
  ——此前这两处均因**子串匹配产生假阴性**，未能拦截上述真实缺陷；另新增 anchors 多锚点与
  **md→JSON 派生一致性**（lessons 数 / 逐课时时间合计 / LOS 成对 / 目标矩阵 / 探究活动 /
  分层作业 / 锚点 / 行为支持卡 / 泛化 / 安全替代 / 材料 / 落盘样例≡派生）。
- 消除脚本内版本硬编码：改从 SKILL.md frontmatter 读取（`read_engine_version`），升版无需改脚本；
  新增"派生器 ENGINE_VERSION 与引擎一致"断言。
- 本轮基准校验：**118/118 = 100% 通过**。

### 版本说明
- Schema 结构变更（`anchor` → `anchors`）对结构化输出消费方为破坏性变更，**按 SemVer 升次版本 2.3.0**。

## [2.2.0] - 2026-09-23

> 三视角（资深特教专家 / 排版设计师 / Agent Skill 流程架构师）多轮回测修订，针对 2.1.0 自评三处扣分项
> （跨课时行为干预未细化、无障碍字号与对比度缺失、结构化输出无 Schema）做定向补强。

### 新增（特教视角：跨课时行为干预递进，P0）
- **§0 新增"跨课时行为干预递进（PBS）"整机制**：ABC 简录（前因→行为→后果，禁"情绪失控/不配合"等评价词）、
  替代行为须先教先练 ≥2 次且功能对等（DRA/DRI）、逐课时强化削弱（课时1 连续强化→课时2 固定比率 FR2→
  课时3 变动强化＋社会性强化、物质强化淡出、设轮换清单防饱和）、干预一致性由"跨课时行为支持卡"固化。
- **晋级/降级阈值定量化**：达成率 ≥80% 且独立尝试成功率 ≥80% 连续 2 课时 → 降一级支持（F→P→G→V→I），
  降级后观察 1 课时、回落回退一级；连续 2 课时 LOS 无晋级 → 回查策略路由并小步子步幅减半；
  危机（自伤/攻击/长时间逃离）→ 降刺激＋冷静角＋任务降级＋移除围观，**四条红线：禁体罚、禁惩罚性隔离、
  禁强制进食、禁当众批评**，事后 ABC 复盘更新支持卡。
- **修正 LOS 语义缺陷（P1）**：原表述把"无参与(N)"排在支持强度递增序列内，属概念错置。现明确
  N 不属强度序列，表示未进入参与 → 触发优先参与目标＋环境调整，**不计入该生本课学业达成判据**，
  并规定改用参与性判据（注视时长/触碰次数/在场时长）单独记录。
- 环节 0 基线由 6 项扩为 **7 项**（新增"危机处置预案"）；Gate-B 学情最小需求单增补"行为危机史与强化物"；
  环节 4 要求课后出**下一课时支持调整决议**，多课时不得沿用旧档不调；环节 5 AAR 与沉淀件增列"行为干预实效与 ABC 复盘结论"。

### 新增（排版视角：无障碍基线，P1）
- §4 新增**无障碍基线**：字号下限（正文≥12pt、表格≥9pt、脚注≥8pt；学生可视材料图卡标签≥24pt、
  课题与关键句≥36pt，按教室后排 3m 可读反推）；对比度 **≥7:1 WCAG AAA**、任何元素不低于 4.5:1，
  禁浅灰字、禁在图案上叠印文字；**色彩不得仅依赖颜色区分**（禁红绿并列作唯一区分），须叠加形状/符号/文字标签；
  学生可视材料用黑体无衬线、禁书法与装饰体；字距≥1.0、行距 1.5~1.75、单行≤30 字（投屏≤20 字）；
  线条≥1.5pt、关键笔画≥3mm。教师 30 秒自查同步增至 9 项。

### 新增（架构视角：结构化输出契约，P1）
- 新增 **`docs/output-schema.json`**（JSON Schema draft 2020-12）：顶层 `meta/anchor/lessons[]/los_table/
  support/generalization/safety_alternatives/materials/privacy` 必填；`timeline[].step` 枚举 `B|O|P前|P参|P后|S`；
  硬约束 `sum(timeline[].minutes) == meta.单课时时长分钟`（逐课时独立）、`len(lessons) == meta.课时数N`、
  `behavior_card` 六要素、`generalization` 三场景、学生代号 `^生\d+$`、`privacy.red_zone_free = true`。
- 新增 **`examples/好吃的水果_结构化输出样例.json`**：忠实对应 3 课时基准教案的可验证样本（分课时 timeline、
目标矩阵 9 格、LOS 12 人逐课时成对、探究活动、行为支持卡与安全替代表）。
- §6 以指针方式固化契约，SKILL.md 不承载 Schema 全文（保持运行态精简）。

### 修订（可复现性与闸门硬化，P1/P2）
- **BOPPPS 时长换算显式化**：原仅写"其他时长等比换算"，不同模型结果漂移。现给 25′/35′/40′ **三档固化表**
  ＋"取最近档后以 P参 吸收尾差"规则，分钟数一律整数。
- **自检闸门硬化**：明确"自检未全过不得进入环节 3.5 交付"；新增"自检连续 2 轮不过同一条目→直接标待替换项
  交人工，不再回炉"。
- 沉淀件时效表述明确化：须标注**引擎版本与记录日期**，超 2 个月跨班复用须现场复核。

### 修订（回测发现并修复）
- **P1 真实缺陷**：两篇基准示例教案头存在**重复分隔行**（导致转 docx 出现多余表头行/空行），已删除并新增回归断言
  "表格内无重复分隔行"。
- 两篇基准示例补齐：跨课时行为干预支持卡（六要素）＋支持调整判定阈值表（晋级/降级/无进展/无参与/危机）
  ＋排版与无障碍执行说明章节；危机禁忌改为逐项列举（防滑读）。
- `docs/regression_check.py` 扩充至 **88 项断言**：新增行为干预六要素/强化削弱/阈值 80%/四条危机禁忌、
  无障碍基线要素、表格重复分隔行、**结构化输出契约校验**，以及**引擎自身防漂移校验**（frontmatter 上架字段、
  SemVer、版号四处一致、五条铁律表述、新机制保留、外部引用存在性）。
- 本轮基准校验：**88/88 = 100% 通过**。

### 兼容性说明
- 保持"六环节＋双 Gate＋四视图＋双格式"主线向后兼容；2.2.0 全部为增量能力（行为干预、无障碍、Schema），
  单课时流程不受影响（多课时必需 behavior_card，单课时可并为一段但六要素不得缺）。

## [2.1.0] - 2026-09-23

### 新增（多课时模型，P0）
- 引擎新增**课时模型（1~N 课时）**：默认 1 课时，内容量超单课时承载或用户要求时按 N 课时（2~3 为主，须论证）设计，N 随 Gate-A 一并确认。
- 明确多课时硬规则：每课时自成闭环（独立目标矩阵分表、独立五列表含 BOPPPS 六步与 ≥1 探究活动、独立前后测、独立分层作业与板书）；跨课时先出"课时总体安排表"；上一课时后测达成＝下一课时起点；**每课时时间独立合计＝该课时时长（禁跨课时相加）**。
- **LOS 变化记录表逐课时成对列** `学生｜课时N起始｜课时N达成｜…｜备注`，禁漏列、禁只记首尾课时（回测中发现的真实缺陷）。
- 环节 1 多课时先出课时总体安排表；环节 0 Gate-A 展示内容增加"课时切分建议及依据"。

### 新增（交付模板与排版规范，P1）
- 五列表"教学环节"列格式统一 `环节名（步别·X′）`；表尾注明分钟合计。
- 配套件模板固化，禁退化：板书按版式代码块输出（课题词卡/图卡并排/步骤箭头/关键句）；安全替代表固定三列 `环节/材料｜风险｜安全替代`；材料清单逐条 `☐` 勾选框＋可采购措辞。
- 排版基线新增（排版设计师视角）：表格**必有表头行**（教案头用 `| 项目 | 内容 |`）；章节中文数字编号＋表号连续无跳号；五列表列宽比例与防溢出规则（超限时改横向页或降小五）；md 源稿封面**两行一级标题连续相邻**（禁空行）。
- 教务归档视图**必含课标锚点三级**（铁律 4 跨四视图一致）；待替换项清单统一置于教师全量版文末"附：待替换项与假设清单（教师版使用，不随归档）"，归档视图不出现。
- 允许沿用历史锚定单/上一课学情，但须标注来源并注明"课前现场核对"（铁律 2 增补，非编造）；A/B/C 层内个体 LOS 档差可在 LOS 表标注（如 A-、C-），不改三层结构。

### 修订（回测轮次 2：回归基准示例回改）
- `examples/认识5_教学设计方案_2课时.md`、`examples/好吃的水果_教学设计方案_3课时.md`：修复 LOS 表漏列（补逐课时起始/达成成对列）；教案头与教务归档表补表头行；五列表环节列改统一格式；安全替代表补风险列；材料清单改 ☐ 勾选；板书改版式代码块；归档视图补课标锚点三级；封面两行标题改为相邻。

### 新增（回归工具）
- `docs/regression_check.py`：教案产出回归校验脚本（封面标题/表头行/五列表数量/BOPPPS 六步/每课时时间合计/探究活动/LOS 成对列与全生覆盖/材料勾选/安全表三列/板书版式/归档锚点三级/表号与章节号连续/占位符/红区扫描，共 41 项断言），结果输出 `docs/regression_report.txt`。
- 本轮基准校验：**41/41 = 100% 通过**。

### 兼容性说明
- 保持"六环节＋双 Gate＋四视图＋双格式"主线，向后兼容；单课时流程不受影响，多课时为增量能力。

## [2.0.0] - 2026-09-16

### 变更（极限压缩重构）
- SKILL.md 由 519 行压缩至约 120 行（-77%）：删除治理类章节（原 §3 授权引用 /
  §4 快速开始 / §9 回归测试 / §10 版本规范 / §11 贡献 / §15 上架指引 / §16 版本历史），
  改为 §6 治理指针，内容由 LICENSE / CONTRIBUTING / docs / CHANGELOG 外部文件承载。
- 运行态规则去重定于单一定义点：红区护栏（6 处→铁律 1）、双 Gate 协议（6 处→低打扰协议
  ＋环节 0）、35′时长约束（6 处→铁律 5）、占位符规则（5 处→合并）、元信息清理与两行标题
  （4 处→环节 3.5）、Word/PDF Single Source（5 处→§0 定位）。
- 附件 10 类映射表与 JSON Schema 压缩为行内要点；锚定单模板/状态机表压缩为 §5；
  LOS 六级与勾选样式并入使用处；环节 3 多张检查清单合并为一张出稿自检。
- 硬约束零丢失：红区零输入 / 学情禁编造 / 教材禁杜撰 / 课标锚点三级 / 时长恒等 /
  P参≥50% / 探究活动硬要求 / C 层合法参与证据 / AAC / 安全替代 / 家庭包容 / PBS /
  4×3 路由矩阵 / 四视图全部保留。
- V1.1 原文存档 `docs/SKILL_v1.1_archived.md`（可回滚、旧章节号可检索）；
  README 同步 2.0.0 并修正 examples 引用（移除不存在的《5的认识》示例文件引用）。

### 校订（2026-09-18，并入 2.0.0）
- frontmatter 合规修正：版本号改为 SemVer 三段格式（2.0 → 2.0.0）；新增
  `display_name_en`（英文展示名）；中文描述迁移至 `description_zh`；新增 `description_en`
  （英文描述），满足 Skill 市场上架字段校验。

### 兼容性说明
- 功能主线"六环节＋双 Gate＋四视图＋双格式"不变，向后兼容；因文件结构重构，升大版本 2.0.0。

## [1.1.0] - 2026-09-16

### 新增
- 环节 0 新增 Gate-A 前置「0-B 步·教材内容匹配」（强制）：严格依据用户输入
  （学科/年级/课题/教材版本线索）或环节 0.5 附件识别回填（教材页/课文原文/课标文件线索）
  深度检索并最大限度匹配教材/教参/教学内容，输出到「教材版本 → 册次 → 单元 → 课」粒度
  （可联动课标锚点三级）；以公开教材目录/教参/课标为依据，禁止杜撰或幻觉教材信息。
- 匹配不确定项一律列入「待替换项」并作为 Gate-A 确认内容呈现（轻确认句式），
  不得静默编造；匹配失败按「合理默认＋假设清单」推进并在 Gate-A 明示「教材未确证」。
- 内置两则已确证教材匹配示例：
  - ① 人教版培智生活语文二年级上册第1课 → 人民教育出版社培智《生活语文》
    二年级上册 第一单元《学校生活》第1课《好学生》；
  - ② 岭南版美术二年级上册第3课 → 岭南美术出版社艺术（造型美术）二年级上册
    第一单元《谢谢你》第3课《相亲相爱一家人》。
- 会话锚定单模板新增「环节0教材匹配」记录行；运行状态机「等待·Gate-A」行为
  补充展示教材匹配结论（版本→册次→单元→课）。

### 修复
- 无。

### 兼容性说明
- 保持"六环节＋双 Gate＋四视图＋双格式"主线不变，向后兼容；仅增量新增
  Gate-A 前置教材内容匹配步骤（0-B 步），红区零输入护栏与双 Gate 停等不削弱。

## [1.0.0] - 2026-09-15

### 新增（首个正式发布）
- 六环节引擎（0 双脉诊断 / 1 锚定目标 / 2 拆解生成 / 3 研判精修 / 4 课堂实施 / 5 复盘写库），
  另含辅助环节 0.5 附件摄入与内容识别、3.5 格式交付。
- 双 Gate 低打扰确认协议：Gate-A（授课内容理解，不确认不继续）＋ Gate-B（学情线索，
  具备即通过、不具备硬性停等）；其余环节低打扰推进。
- 四视图一次生成：教师一页速览版 / 教师全量版 / 教务归档版 / 家校反馈版。
- 双格式交付（Single Source）：Word（.docx 可编辑）＋ PDF（.pdf 打印/归档），
  md 为唯一源稿，§13.1 成品格式基线规格表统一执行。
- 红区零输入护栏（零输入零输出）：学生姓名/身份证/病历/照片/家庭敏感结构
  一律不识别、不输出、不进入成品；附件摄入前置红区检测。
- 4×3 知识类型 × 学科属性动态策略路由（第一性原理拒绝模板）。
- LOS 六级官方连续体：独立(I) → 口头(V) → 手势(G) → 身体(P) → 完全辅助(F) → 无参与(N)。
- BOPPPS 六步参照模型（导入 B / 目标 O / 前测 P前 / 参与式学习 P参 / 后测 P后 / 总结 S），
  参与式学习必须内嵌 ≥1 个学生可操作探究活动且为时长最大环节（≥50%）。
- 示例教案归档：`examples/好吃的水果_教学设计方案.md`（实跑验证）。
- 开源配套：README.md / LICENSE（MIT＋红区护栏不可移除附加条款）/
  CONTRIBUTING.md / docs/release-checklist.md / .gitignore。

### 修复
- 无（首个正式发布）。

### 兼容性说明
- 0.x 各版本保持"六环节 + 双 Gate + 四视图 + 双格式"主线，向后兼容。
- 1.x 各版本均保持该主线，向后兼容；跨主版本变更将在 README 标注迁移说明。
