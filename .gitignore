# —— 培智·双脉教学引擎 · Git 忽略规则 ——
# 依据 SKILL.md §6（治理与外部文件）与 references/release-checklist.md 制定
# 说明：2.5.0 起双轨制——仓库保留治理文件（GitHub 轨）；
#      上传 zip 由 scripts/build_package.py 产出到 dist/（技能轨，不上传仓库）；
#      运行期报告一律写系统临时目录 peizhi_shuangmai/，不进仓库。

# 1. 交付成品（会话产出的 Word/PDF 一律不入库；2.5.0 起 examples/ 不再保留 docx 基准，
#    "成品≡源稿"端到端校验由回归脚本在临时目录执行）
*.docx
*.pdf

# 1b. 打包产物（技能上传 zip，本地生成，不入库）
dist/

# 2. 会话中间产物（锚定单 / 内容提取卡 / 设计底稿）
锚定单_*.md
内容提取卡_*.md
*_设计底稿*.md

# 3. 复盘与写库沉淀（策略库 / 话术库 / 材料库 / 复盘记录）
方法论库/
*复盘*.md

# 4. 工作目录临时文件
temp/
output/
*.tmp
*.bak
*.swp
*~

# 5. Python / 环境缓存
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# 6. 编辑器与系统文件
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# 7. 其他敏感或无关文件
*.key
*.pem
*.log

# 8. 本地工具内部记忆（WorkBuddy 等，不上传）
.workbuddy/
